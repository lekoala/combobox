{
  "name": "@lekoala/combobox",
  "version": "0.1.0",
  "description": "Native-first <combo-box> combobox/filterable-select",
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.4.0",
  "keywords": [
    "combobox",
    "select",
    "filterable-select",
    "datalist",
    "custom-select",
    "popover",
    "web-component"
  ],
  "files": [
    "dist",
    "src",
    "LICENSE",
    "README.md"
  ],
  "exports": {
    ".": "./src/index.js",
    "./define": "./src/define.js",
    "./combobox.css": "./src/combobox.css",
    "./dist/combobox.js": "./dist/combobox.js"
  },
  "scripts": {
    "build": "bun build src/define.js --outfile=dist/combobox.js --format=iife",
    "dev": "bun run build && node test/server.js",
    "test": "bun run test:unit && bun run test:browser && bun run test:dist",
    "test:unit": "bun test test/unit",
    "test:browser": "playwright test test/browser",
    "test:browser:headed": "playwright test test/browser --headed",
    "test:browser:chromium": "playwright test test/browser --project chromium",
    "test:browser:matrix": "playwright test test/browser --project firefox --project webkit --workers 3",
    "test:matrix": "bun run test:browser:matrix && bun run test:dist --project firefox --project webkit --workers 3",
    "test:dist": "playwright test test/dist",
    "pretest:dist": "bun run build",
    "test:syntax": "node --check src/combobox.js && node --check src/combo-box.js && node --check src/helpers.js && node --check src/index.js && node --check src/define.js",
    "lint": "biome check src test playwright.config.js",
    "format": "biome format --write src test playwright.config.js",
    "check": "bun run test:syntax && bun run lint && bun run test:unit && bun run build && bun run test:browser --project chromium && bun run test:dist --project chromium",
    "prepack": "bun run build",
    "prepublishOnly": "bun run lint && bun run test:unit && bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@playwright/test": "^1.55.0"
  }
}